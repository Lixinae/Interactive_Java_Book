<?xml version="1.0" encoding="UTF-8"?>
<project name="Interactive_Java_Book" default="jar" basedir=".">
	<description>
		Build file for Interactive_Java_Book
	</description>
	<!-- set global properties for this build -->
	<property name="src.dir" location="src"/>
	<property name="build.dir" location="classes"/>
	<property name="dest.dir" location="dest"/>
	<property name="lib.dir" location="libs"/>
	<property name="docs.dir" location="docs"/>
	<property name="jdk1.8" location="~/jdk1.8.0_65"/>


	<!--  Create a classpath container which can be later used in the ant task -->
	<path id="build.classpath">
		<fileset dir="${lib.dir}">
			<include name="**/*.jar" />
		</fileset>
	</path>

	<!-- Clean all the generated files -->
	<target name="clean" description="Removing the all generated files.">
		<delete dir="${build.dir}"/>
		<delete dir="${dest.dir}"/>
	</target>

	<!-- Cleans the folder and creates the necessary directorie -->
	<target name="prepare" depends="clean">
		<mkdir dir="${build.dir}"/>
		<mkdir dir="${dest.dir}"/>
		<mkdir dir="${src.dir}"/>
	</target>

	<!-- Compile the program -->
	<target name="compile" depends="prepare" description="Compilation of all source code.">
		<javac srcdir="${src.dir}"
			   destdir="${build.dir}"
			   fork = "true"
			   executable="$(jdk1.8)/bin/javac"
			   taskname="javac1.8"
			   includeantruntime="false"
		/>
	</target>

	<!-- Creates Javadoc -->
	<target name="docs" depends="compile">
		<javadoc packagenames="src" sourcepath="${src.dir}" destdir="${docs.dir}">
			<!-- Define which files / directory should get included, we include all -->
			<fileset dir="${.dir}">
				<include name="**" />
			</fileset>
		</javadoc>
	</target>

	<target name="jar" depends="compile" description="Generates jshellbook.jar file in to the 'dest' directory.">
		<jar jarfile="${dest.dir}/jshellbook.jar" basedir="${build.dir}">
			<manifest>
				<attribute name="Interactive_Java_Book" value="InterJavaBook.Main" />
			</manifest>
		</jar>
	</target>


</project>